0.0.6
- Camera FOV can now be manipulated during cutscenes
- Added screen shake tracks and keyframes
- Global screen shake intensity can be set in config
- Fixed a time calculation issue in cutscenes that caused them to constantly stutter slightly. All cutscenes should now be much smoother
- Fixed a possible crash when a continuous track has a single keyframe with smooth interpolation
- Sound keyframes now support positional sounds (sounds at actor positions will be supported once actors have been implemented)
- Moved ModTrackTypes into the registry package
- Player hand is no longer rendered during cutscenes
- Player model rendering can be switched on and off for cutscenes (off by default)
- Camera pitch is now set in computeCameraAngles rather than the track itself
- Camera entity now has a specific UUID to avoid potential collisions
- Track.keyframes is now protected instead of private
- Added mod logo to asset files


0.0.5
- Cutscenes can now be played
- Added a CutsceneManager to hold scheduled cutscenes as well as begin, end and progress them
- Added keyframe and track types for sounds (doesn't support positional sounds yet)
- Cutscenes can now be properly triggered by the run_cutscene command
- Added targets argument to run_cutscene command
- Added a copy method for keyframes
- Removed an unnecessary portion of static code from CameraPositionKeyframe
- Removed tick field from Cutscene
- Fixed a possible division by 0 in continuous track types if 2 sequential keyframes have a time value of 0 (including duplicated ends)
- Actually added a mod registry for entities
- Camera rotation in keyframes is now stored as a Vec3 instead of a Quaternionf
- Added a static EMPTY field for Cutscenes and CutsceneDatas
- Camera rotation now has the same JSON format as camera position
- Added a second test cutscene


0.0.4
- Added datapack support for cutscenes
- Added test_cutscene.json
- Added codecs for CutsceneData, Tracks, Keyframes and Quaternions
- Added a Camera Rotation Track
- Added registries for both Track types and CutsceneData instances (this allows new track types to be added by other mods and for easy lookup of cutscenes)
- Added a command to run cutscenes (doesn't yet actually run the cutscene, just pulls the data)
- CutsceneCameraEntity now has a Cutscene field
- Simplified KeyframeUtil.getOrientedRotation
- Renamed name field in CutsceneData to displayName
- Track array in CutsceneData is now instantiated in the constructor
- CutsceneData field in Cutscene is now final (and now has a getter)
- Fixed keyframes still storing timings as ints
- Moved neoforge.mods.toml back into templates directory


0.0.3
- Added CameraPositionTrack
- Added CutsceneCameraEntity
- Fleshed out Track class
- Track class is now abstract, additional functionality for special track types will be added in their own classes
- Keyframe timing is now stored as a float
- Removed redundant fields and methods from Cutscene
- Added startTime field to Cutscene to allow for cutscene scheduling
- Heavily reduced clutter in selected current keyframes for interpolated tracks
- CutsceneData no longer stores any keyframes directly
- Added docstrings for various methods
- Changed message in client setup
- Moved META-INF package into resources package

0.0.2
- Added changelog.txt
- Implemented KeyframeUtil.getOrientedRotation
- Added abstract class Keyframe
- Added class CutsceneData
- Added the beginning of the Track system
- Added logic for current keyframes

0.0.1
- Initial commit
